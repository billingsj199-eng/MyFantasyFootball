# Secrets — NEVER commit
cfbd_config.py

# Backup files (all variants: .bak, .bak_pre_xxx, .bak_round_xxx, etc.)
*.bak*
*.null_backup
backups/

# Python
__pycache__/

# One-off / debug working files
_joined.js
__index_pre.html
__patched_for_extract.html

# Large external data pulls (regenerable via scripts)
scripts/_nflverse_rosters/
scripts/_nflverse_schedules.csv
.nflverse_cache/

# Intermediate BBM aggregation outputs (regenerated by aggregate_* / _emit_* scripts)
scripts/_bbm_*.json

# Obsolete pre-split data (replaced by weekly_stats_retired_1/2/3.js)
*.REPLACED_BY_SPLITS
