# live-tracker-logs

Results branch for .github/workflows/live-tracker.yml (scripts/live_tracker). data/live_track_*.csv = one row per player per minute, *_summary.json = grades vs finals, data/live_claims.json = games claimed by running jobs. Never merged into main.
